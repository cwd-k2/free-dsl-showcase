{
  "compilerOptions": {
    "strict": true
  },
  "fmt": {
    "lineWidth": 100
  },
  "tasks": {
    "check": "deno fmt --check && deno lint && deno check src/*.ts tests/*.ts",
    "test": "deno test"
  }
}
